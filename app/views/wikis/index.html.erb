<h1>All Wikis</h1>

<% @wikis.each do |wiki| %>
  <p><%= link_to wiki.title, wiki %></p>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= link_to wiki.title, wiki %>
       </h4>
     </div>
   </div>
 <% end %>
    
